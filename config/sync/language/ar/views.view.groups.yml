display:
  default:
    display_options:
      exposed_form:
        options:
          reset_button_label: Reset
      arguments:
        group_membership_or_creator:
          exception:
            title: All
